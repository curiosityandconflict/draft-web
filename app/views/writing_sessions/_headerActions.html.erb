<% if @session.new_record? %>
  <%= link_to 'Cancel', writing_sessions_path, class: 'header-action', data: { 'turbo-action': 'restore' }  %>
<% else %>
  <%= link_to 'Show', @session, class: 'header-action', data: {'turbo-action': 'replace'} %>
  <%= link_to 'Back', writing_sessions_path, class: 'header-action', data: {'turbo-action': 'restore'} %>
<% end %>